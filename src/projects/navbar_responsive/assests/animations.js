import gsap from 'gsap';

export const loadApp = (tl = gsap.timeline({autoAlpha:0})) => {
    tl
    
    ;
    return tl;
};

export const toggleModal = ( tl = gsap.timeline({autoAlpha:0}) ) => {
 
    tl
    
    ;
   
    return tl
}
